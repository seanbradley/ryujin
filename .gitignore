# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Shell scripts (except deploy/ and connect.sh)
*.sh
!deploy/*.sh
!connect.sh

# ChromaDB (all locations)
/chroma_db/
/chroma_db/*
/data/chroma/
/data/chroma/*
*.sqlite3
*.bin

# SQLite databases and WAL/SHM sidecar files
/data/commitment_ledger.db
/data/*.db
/data/*.db-shm
/data/*.db-wal

# Editor backup files (tilde-suffix and BACKUP.txt — review before deletion)
*.BACKUP.txt
*~

# Test/screenshot images — never commit large binaries to git
docs/images/*.jpg
docs/images/*.jpeg
docs/images/*.png
docs/images/*.webp
docs/images/*.pdf
docs/images/*.mp4

# Docs-root scratch images (e.g. disaster.png)
docs/*.png
docs/*.jpg

# Research binaries — PDFs and screenshots are reference-only, not source artifacts
docs/research/*.pdf
docs/research/*.png
docs/research/*.jpg

# Branding WIP images not served by the UI
web/static/img/pack0x00*.png

# Cursor reference docs (not project rules)
.cursor/bugbot.md

# Tactical manuals — PDFs are build-time inputs for RAG indexing, not source artifacts.
# The vector store (ChromaDB) is the source of truth after indexing.
# See docs/tactical_manuals/SOURCES.md for the canonical publication list + fetch URLs.
# See scripts/index_manuals.py to rebuild the index.
docs/tactical_manuals/*.pdf
docs/tactical_manuals/*.PDF

# Logs and test run output
logs/
*.log
tests/fixtures/bare_claude_responses.json

# Agent transcripts — committed to repo (~18 MB total, within GitHub limits)
docs/transcripts/cursor-kv/
docs/transcript_exports/

# Metrics and Backups
archive/*
backups/*
*.bak
*.old

# Django stuff:
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments & Secrets
.env
.env.*
.env.ini
*.env
.venv
env/
secrets.py
credentials.json
*_secrets.*
*_credentials.*
*.pem
*.key
venv/
ENV/
env.bak/
venv.bak/
.bak
.old
*~

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# Cowork: browser-saved pages, video assets, and large images (do not commit to repo)
docs/cowork/wolf-you-feed-combined-script.md
docs/cowork/wolf-you-feed-video-scripts.md
docs/cowork/content/notebooklm/
docs/cowork/content/**/*.mp4
docs/cowork/content/**/*.mov
docs/cowork/content/**/*.avi
docs/cowork/content/infographics/
docs/cowork/**/*.png
docs/cowork/**/*.jpg
docs/images/**/*.png
docs/images/**/*.jpg
docs/cowork/content/research/

# Provider pricing / rate-limit reference screenshots (large PNGs, local-only).
# Binary assets are not committed (see no-binary rule); keep VC clean.
docs/prices_and_limits/

# Ad-hoc read-only diagnostic scripts (local-only; keep VC clean).
# Tracked reference templates (e.g. _inspect_matchmaker_state_ro.py) are
# explicitly re-included below.
scripts/_diag_*_ro.py

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# --- RYUJIN: local-only working material (not part of the public proposal repo) ---
# Personal scratch / thinking, SOCOM SOFWORX RFI package, pre-submission checks,
# the internal gap+teaming memo, byzantine-method scratch notes, and SAM.gov
# registration PDFs all stay local. They remain on disk; git just stops tracking.
docs/sim_thoughts.txt
docs/SOFWorx/
docs/abstract_check.txt
docs/B_gap_and_teaming.md
docs/bysantiine_solutions.md
docs/SAM.gov*

# LibreOffice / OpenOffice lock files left next to open .docx/.xlsx templates
.~lock.*#
